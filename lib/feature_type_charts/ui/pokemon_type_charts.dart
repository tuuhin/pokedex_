import 'package:flutter/material.dart';

class PokemonTypeChats extends StatelessWidget {
  const PokemonTypeChats({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('types chart'),
      ),
    );
  }
}
