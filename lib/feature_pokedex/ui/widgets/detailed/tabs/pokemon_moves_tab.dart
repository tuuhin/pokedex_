import 'package:flutter/material.dart';

class PokemonMovesTab extends StatelessWidget {
  const PokemonMovesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("moves"),
    );
  }
}
